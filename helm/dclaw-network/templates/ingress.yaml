apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dclaw-network.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-network.name" . }}
spec:
  rules:
    - host: dclaw-network.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "dclaw-network.name" . }}
                port:
                  number: {{ .Values.frontend.service.port }}
